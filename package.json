{
  "name": "luxi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "cd react-app && npm ci && npm run build",
    "preview": "vite preview",
    "dev:react-app": "cd react-app && npm run dev",
    "start:react-app": "cd react-app && npm start",
    "start:backend": "cd backend && npm start",
    "install:all": "npm install && cd react-app && npm install && cd ../backend && npm install",
    "build:frontend": "cd react-app && npm ci && npm run build",
    "build:backend": "cd backend && npm ci"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.87.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.9"
  }
}
